const prompt = require('prompt-sync')();

//Crie uma lista com os nomes de três amigos e imprima cada nome.

let nome = ['Julia', 'Leticia', 'Gabi']
for (let nomes of nome) {
    console.log(`O nome é ${nomes}`);
    
}